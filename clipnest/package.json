{
  "name": "clipnest",
  "version": "1.0.0",
  "private": true,
  "description": "Local browser clipboard history for copied text, links, and images.",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "license": "MIT",
  "type": "commonjs"
}
